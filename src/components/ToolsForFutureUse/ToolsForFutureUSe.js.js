
// Used to make sure pTally card does not match any common card values (player 1)
// && props.commonCards.cards.map((card) => {return(card.value)}).indexOf(props.p1Tally.pCardValue.value) === -1

// export function createCommonCards(commonstate) {
//     return commonstate.map((card, idx) => {
//         new Card (card.code, card.image, card.value, card.suit, idx)
//     })
// }

// export default Card